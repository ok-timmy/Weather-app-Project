//import LoadWeatherAPI from './component';
import LoadWeatherAPI from './component'

function App() {
  return (
    <div className="app">
     <LoadWeatherAPI />
    </div>
  );

}

export default App;


// TODOS: Set a display when we are still getting our data,
// Make the search form become functional.
// set an error message to display if the city name we are searching for can't be found.
// Set the style sheet to be mobile responsive.
// 
